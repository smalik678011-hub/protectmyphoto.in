// Copy this file to firebase-config.js and fill values from your Firebase project.
// Firebase web config is public project routing data, not an admin secret.
// Never add service-account JSON, private keys, SMTP passwords, or admin SDK keys here.
export const firebaseConfig = {
  apiKey: "YOUR_FIREBASE_WEB_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_FIREBASE_APP_ID"
};
